import success from './copyClipboard.js'
import discord from './copyClipboard.js'
function appendText() {
    var text = document.createTextNode('test');
    console.log(discord);
    if (success = 1) {
        discord.appendChild(text);
    };
}